#[allow(dead_code)]
pub struct SubprocessAgent;
// Phase 2: tokio::process::Command with generic (command, args).
// Anthropic: ["claude", "-p", prompt]
// OpenCode:  ["opencode", "run", "--print", prompt]
